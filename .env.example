# 简历工坊 · 环境变量示例
# ------------------------------------------------------------------
# 本文件只是「示例/占位」，不含真实密钥。
# - 本地开发：把需要的变量在启动命令里带上（本服务端零依赖、不自动读 .env）。
#     Windows PowerShell:
#       $env:SUPABASE_URL="https://xxxx.supabase.co"; $env:SUPABASE_ANON_KEY="eyJ..."; node server.js
#     macOS / Linux:
#       SUPABASE_URL="https://xxxx.supabase.co" SUPABASE_ANON_KEY="eyJ..." node server.js
# - Vercel 上线：在 Project Settings → Environment Variables 里配置下面这两个
#   同名变量即可，运行时会自动注入。切勿把真实密钥提交进 Git。

SUPABASE_URL=https://替换成你的项目-ref.supabase.co
SUPABASE_ANON_KEY=替换成你的_anon_公钥

# 下面两个仅本地 `node server.js` 才用到；Vercel 上无需设置。
PORT=8081
HOST=127.0.0.1
